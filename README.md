# Sign Image Model (from-scratch CNN)

Goal: classify static sign images (e.g., ASL alphabet) using a **custom CNN** (no pretrained weights).

## Data layout
data/
  train/<label>/*.jpg
  val/<label>/*.jpg